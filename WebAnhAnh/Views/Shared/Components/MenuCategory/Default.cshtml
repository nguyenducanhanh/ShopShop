@model IEnumerable<WebAnhAnh.Repository.MenuCategoryRepository>

 

<style>
    .navbar {
        background-color: pink; /* Màu nền đen cho thanh menu */
    }

    .navbar-nav .nav-item .nav-link {
        border-radius: 10px; /* Border tròn */
        margin: 0 10px; /* Khoảng cách giữa các menu */
        transition: background-color 0.3s; /* Hiệu ứng khi hover */
        background-color: pink;
    }

        .navbar-nav .nav-item .nav-link:hover {
            background-color: #1bc8bb9c ; /* Màu nền khi hover */
        }
</style>

              


 <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
       

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                @foreach (var item in Model)
                {
                    <li class="nav-item">
                        <a style="font-size: 15px; color: black; font-family: 'Helvetica', sans-serif; font-weight: 500; text-decoration: none;" asp-controller="Product" asp-action="Index" asp-route-category="@item.CategoryId" class="nav-link"> <div class="row"> @item.CategoryName (@item.Quantity)</div></a>
                    </li>
                }

            </ul>
        </div>
    </div>
</nav> 


